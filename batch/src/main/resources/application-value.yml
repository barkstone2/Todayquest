batch:
  context:
    target-achievement-key: "targetAchievement"
    achieved-logs-key: "achievedLogs"
    notified-user-ids-key: "notifiedUserIds"
  param:
    target-achievement-id-key: "targetAchievementId"
aws:
  sqs:
    queue:
      batch-job-queue-url: ${AWS_SQS_BATCH_JOB_QUEUE_URL}